{% extends('base.html.twig') %}

{% block title %}
	<title>buscar imgs</title>
{% endblock %}

{% block body %}
	{{ parent() }}
	<div class="container text-center">
		<div class="d-inline-flex p-3 bg-secondary text-white">
		{# <input type="color"></input> #}

			{{form_start(form)}}
				 <div class="form-group">
	              	<h5>Buscar IMAGENES por distancia de COLOR</h5>
		         </div>
			     <div class="form-group">
			     	{{form_row(form.color)}}<b id="valorcolor"></b>
			     </div>
			     <div class="form-group">
			     	{{form_row(form.distancia)}}<b id="valordistancia"></b>
					
			     </div>
			     <div class="form-group p-2">
				 {{form_widget(form.submit)}}
			{{form_end(form)}}
		</div>
	</div>


	{% block javascripts %}
		<script src="{{ asset('assets/js/javascript1.js') }}" type="text/javascript"></script>
        <script>
           funcion3(); 
        </script>

	{% endblock %}


{% endblock %}
